import React from "react";

const Header = () => (

    <header className="z-50 h-16 w-full bg-grey-darker shadow flex items-center justify-between">
        <div className="flex items-center h-full">
            <div className="flex items-center text-center h-full w-48 border-r border-grey-dark">
                <a href="/" className="w-full text-white text-sm uppercase font-extrabold">STOCK ASSESSMENT</a>
            </div>

        </div>
    </header>

);

export default Header;
